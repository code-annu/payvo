enum AppRoutes {
  HOME = "/dashboard",
  LOGIN = "/login",
  SIGNUP = "/signup",
  TRANSACTIONS = "/transactions",
  API_KEYS = "/api-keys",
  ACCOUNT_SETTINGS = "/settings",
}

export default AppRoutes;
